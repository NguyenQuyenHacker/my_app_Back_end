# Database chính (FastAPI app — banking_db)
DATABASE_URL=

# Database admin (pgvector cho RAG) — Neon vector_db (chứa admins, knowledge_bases, bảng vector)
DATABASE_URL_ADMIN=

# SQL echo (true cho dev, false cho prod)
DB_ECHO=true

# Google Gemini API key (dùng cho embedding + LLM)
GOOGLE_API_KEY=

# JWT secret key (dùng cho ký access token)
JWT_SECRET_KEY=
